@FFI("C") extern fn printf(fmt: byte&, ...) -> int32
@FFI("C") extern fn atoi(str: byte&) -> int32

struct T
	x: int64
	y: int64
	z: int64

struct S
	a: int32
	b: int32

fn main(argc: uint64, argv: byte&&)
	if argc != 3
		printf("Usage: %s <number> <number>\n", argv^)
	
	# S::h::offset
	# S::e::size
	# uint64::size
	
	let s = S
		a: atoi(argv[8])
		b: atoi(argv[16])
	
	printf("%d, %d\n", s.a+1, s.b+1)
